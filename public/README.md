# Public Assets Folder

Place your landing page background image file here.

### Recommended File Name:
- Save the file as **`background.jpg`** (or `.png`/`.webp`).
- If you use a different format (like PNG), save it as `background.png` and let me know so I can update the CSS file extension.
